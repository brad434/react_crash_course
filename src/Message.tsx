//PascalCasing
function Message() {
  // JSX: Javascript XML
  const name = "";
  //you can add an 'if' statement like so:
  if (name) return <h1>Hello {name}</h1>;
  //if not then do this:
  return <h1>Hello Stranger!</h1>;
}

export default Message;
